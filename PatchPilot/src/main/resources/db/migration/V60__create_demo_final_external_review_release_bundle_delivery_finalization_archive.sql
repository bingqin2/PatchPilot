CREATE TABLE IF NOT EXISTS demo_final_external_review_release_bundle_delivery_finalization_archive (
    id VARCHAR(64) PRIMARY KEY,
    status VARCHAR(32) NOT NULL,
    finalized BOOLEAN NOT NULL,
    summary VARCHAR(1024) NOT NULL,
    next_action VARCHAR(1024) NOT NULL,
    latest_archive_id VARCHAR(64),
    latest_delivery_receipt_id VARCHAR(64),
    latest_certificate_archive_id VARCHAR(64),
    latest_delivery_finalization_archive_id VARCHAR(64),
    latest_package_archive_id VARCHAR(64),
    latest_package_delivery_receipt_id VARCHAR(64),
    latest_task_id VARCHAR(64),
    latest_pull_request_url VARCHAR(1024),
    latest_delivery_target VARCHAR(512),
    latest_delivery_channel VARCHAR(64),
    latest_delivered_at VARCHAR(64),
    release_bundle_delivery_receipt_freshness VARCHAR(32) NOT NULL,
    release_bundle_delivery_receipt_fresh BOOLEAN NOT NULL,
    release_bundle_delivery_receipt_freshness_summary VARCHAR(1024) NOT NULL,
    checks_json JSON NOT NULL,
    evidence_notes_json JSON NOT NULL,
    download_actions_json JSON NOT NULL,
    side_effect_contract VARCHAR(1024) NOT NULL,
    report MEDIUMTEXT NOT NULL,
    generated_at TIMESTAMP(6) NOT NULL,
    archived_at TIMESTAMP(6) NOT NULL,
    INDEX idx_release_bundle_delivery_finalization_archive_archived_at (archived_at),
    INDEX idx_release_bundle_delivery_finalization_archive_status (status)
);
