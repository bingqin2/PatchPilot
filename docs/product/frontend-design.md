# Frontend Design Direction

## Purpose

PatchPilot's frontend is an operations dashboard for maintainers, developers, and demos. Its job is to make the backend workflow visible: which issue triggered a task, what the agent did, where it failed, what tests ran, and which Pull Request was created.

The frontend is not the primary production trigger surface. GitHub issue comments remain the main task trigger, and GitHub Pull Requests remain the review surface. The dashboard may offer a manual task creation form for local demos and debugging, but it still goes through the backend queue.

## Design Philosophy

The interface should feel like an engineering control plane, not a chatbot, marketing page, or generic AI SaaS product. Operators should be able to scan task state quickly, inspect evidence, and jump back to GitHub when human review is needed.

Core principles:

- Prefer dense but readable operational views over large promotional sections.
- Show real task records, not abstract "AI progress" copy.
- Keep controls explicit and limited to safe operations.
- Make failures first-class: failed tests, cancelled tasks, queue errors, and agent exceptions should be easy to inspect.
- Make setup failures readable: backend/proxy connectivity problems should tell operators what to check.
- Preserve GitHub as the source of code review and merge decisions.

## Current Structure

The current frontend lives in `frontend/` and uses React, Vite, and TypeScript.

During local development, Vite proxies `/api` and `/health` to `PATCHPILOT_FRONTEND_BACKEND_URL` or `VITE_PATCHPILOT_BACKEND_URL` from the shell environment or repository root `.env`, defaulting to `http://127.0.0.1:8080`.

When the backend protects operator APIs with `PATCHPILOT_ADMIN_TOKEN`, the dashboard does not require a separate login flow. `App.tsx` shows a compact header control that reports whether this browser has a saved admin token, lets an operator save or replace the local token, and lets the operator clear it when rotating credentials or testing unauthenticated behavior. For trusted localhost-only development, `PATCHPILOT_DASHBOARD_ADMIN_TOKEN_BOOTSTRAP_ENABLED=true` lets `GET /api/dashboard/bootstrap` return the configured admin token before protected API loading starts; the dashboard stores that token only when no browser token already exists. If a protected API call returns `Admin token is required`, the same save-and-retry path appears inline inside the alert. Saved tokens live in browser `localStorage` as `patchpilot.adminToken`; the API helper reads that value for later requests and sends it as `X-PatchPilot-Admin-Token`.

The page coordinator is `frontend/src/App.tsx`. It loads backend data, owns selected-task state, applies status filters, repository owner/name filters, language/build-system adapter filters, created time range filters, task-list sort, and search state, coordinates manual trigger evaluation, manual creation, cancel, and retry actions, and runs local repository preflight diagnostics through `POST /api/repository-preflight`. It loads `/health` and then tries the dashboard bootstrap endpoint before the protected dashboard API batch, so the connectivity panel can distinguish "backend/proxy unavailable" from "backend is up but protected APIs need an admin token." The operator setup checklist is deliberately derived from the same loaded data instead of a new API, so it acts as a first-screen summary of the detailed panels below it.
It also loads `GET /api/demo/evidence-bundle`, `GET /api/demo/session-snapshot`, `GET /api/demo/session-archives`, `GET /api/demo/handoff-package-archives`, `GET /api/demo/handoff-package-archives/summary`, `GET /api/demo/handoff-share-checklist`, `GET /api/demo/handoff-share-center`, `GET /api/demo/handoff-finalization`, `GET /api/demo/handoff-share-instructions`, `GET /api/demo/handoff-share-delivery-receipts`, `GET /api/demo/self-hosted-launch-readiness`, `GET /api/demo/self-hosted-launch-readiness/archives`, `GET /api/demo/launch-evidence-package`, `GET /api/demo/launch-evidence-package/archives`, `GET /api/demo/launch-evidence-share-center`, `GET /api/demo/script`, `GET /api/demo/readiness`, `GET /api/demo/readiness-snapshots`, `GET /api/demo/readiness-snapshots/summary`, `GET /api/demo/smoke-checklist`, `GET /api/github/credential-readiness`, `GET /api/github/webhook-url-readiness`, `GET /api/github/webhook-setup-readiness`, `GET /api/github/repository-access-readiness`, `GET /api/language-adapters`, `GET /api/language-adapters/fixtures`, `GET /api/language-adapters/runtime-readiness`, `GET /api/evaluation/cases`, `GET /api/evaluation/summary`, `GET /api/evaluation/case-readiness`, `GET /api/evaluation/run-preview`, `GET /api/evaluation/fixture-baseline-runs`, `GET /api/evaluation/fixture-baseline-runs/summary`, `GET /api/evaluation/runs`, `GET /api/rejected-triggers`, `GET /api/rejected-triggers/summary`, `GET /api/trigger-quarantines`, `GET /api/admin-audit-events`, and `GET /api/github/webhook-deliveries` so demo evidence, a full demo session snapshot, recent archived session reports, recent archived handoff packages, the latest handoff archive share-ready summary, explicit handoff share checks, final handoff share-center status, final handoff finalization status, final self-hosted launch readiness, recent archived launch readiness packages, the final launch evidence package, recent archived launch evidence packages, the final launch evidence share center, final handoff share instructions, recent handoff share delivery receipts, the read-only demo script, readiness, recent readiness snapshot reports, readiness snapshot trend movement, final smoke-check steps, GitHub token acceptance, public webhook URL reachability, combined webhook setup readiness, selected repository access, supported repository shapes, fixed verification commands, detection signals, demo fixture paths, current fixture pass/fail status, current adapter executable availability, a copyable adapter readiness report, a read-only evaluation case catalog, readiness summary, fixture-readiness report, run preview, recent archived fixture baseline runs, baseline regression movement, and recent archived full evaluation runs, selected accepted-trigger evidence, recent rejected task triggers, rejected-trigger abuse patterns, active trigger quarantines, recent protected admin mutations, recent GitHub delivery outcomes, setup readiness, and redelivery guidance are visible in the dashboard instead of only in source code, terminal smoke output, curl output, or GitHub's delivery page. The demo evidence bundle panel also repeats combined webhook setup readiness with the derived GitHub payload URL, a capped recent webhook delivery trail, the final handoff share-center status plus download actions, final launch evidence share-center status with latest archive/session/Pull Request identifiers and download actions, and the finalization status so launch and handoff evidence do not depend on opening the separate webhook diagnostics, launch package, or session snapshot panels. The self-hosted launch readiness panel combines readiness, evidence-bundle, handoff finalization, credential, webhook setup, and queue/worker checks into the dashboard's final pre-launch readout, downloads the current evidence through `GET /api/demo/self-hosted-launch-readiness/report/download`, archives the current package through `POST /api/demo/self-hosted-launch-readiness/archives`, and downloads historical reports through `GET /api/demo/self-hosted-launch-readiness/archives/{archiveId}/report/download`. The launch evidence package panel combines that self-hosted readiness state with the current session snapshot, live task/Pull Request/webhook proof, evaluation coverage, post-demo proof, next actions, and a read-only side-effect contract, then copies the loaded Markdown report, downloads it through `GET /api/demo/launch-evidence-package/report/download`, archives the current package through `POST /api/demo/launch-evidence-package/archives`, lists recent package archives from `GET /api/demo/launch-evidence-package/archives`, downloads historical reports through `GET /api/demo/launch-evidence-package/archives/{archiveId}/report/download`, and shows the launch evidence share center from `GET /api/demo/launch-evidence-share-center` with a report download backed by `GET /api/demo/launch-evidence-share-center/report/download`. It calls `GET /api/demo/runbook` only when an operator clicks `Copy runbook`, `POST /api/demo/session-report` only when an operator clicks `Copy session report` with prepared command and archived outcome context, `POST /api/demo/session-report/download` only when an operator clicks `Download session report` with prepared command and archived outcome context, `POST /api/demo/handoff-package` only when an operator clicks `Copy handoff package` with prepared command and archived outcome context, `POST /api/demo/handoff-package/download` only when an operator clicks `Download handoff package` with prepared command and archived outcome context, `GET /api/demo/handoff-package-archives/summary-report/download` only when an operator downloads the latest handoff archive summary report, `GET /api/demo/handoff-finalization/report/download` only when an operator downloads final acceptance evidence, `POST /api/demo/handoff-share-delivery-receipts` only when an operator records local delivery evidence, `GET /api/demo/handoff-share-delivery-receipts/{receiptId}/report/download` only when an operator downloads one delivery receipt report, `GET /api/demo/session-archives/{archiveId}/report/download` only when an operator downloads one archived report, `GET /api/demo/handoff-package-archives/{archiveId}/report/download` only when an operator downloads one archived handoff package, `POST /api/demo/session-archives` only when an operator clicks `Archive session`, `POST /api/demo/handoff-package-archives` only when an operator clicks `Archive handoff package`, `POST /api/demo/launch-evidence-package/archives` only when an operator clicks `Archive package`, `GET /api/demo/launch-evidence-package/archives/{archiveId}/report/download` only when an operator downloads one archived launch evidence package, `GET /api/demo/launch-evidence-share-center/report/download` only when an operator downloads the latest launch evidence share-center report, `POST /api/demo/readiness-snapshots` only when an operator clicks `Archive readiness`, `GET /api/demo/readiness-snapshots/{snapshotId}/report/download` only when an operator downloads one archived readiness report, `POST /api/evaluation/fixture-baseline` only when an operator clicks `Run fixture baseline`, `POST /api/evaluation/fixture-baseline-runs` only when an operator clicks `Run and archive fixture baseline`, `GET /api/evaluation/fixture-baseline-runs/{runId}/report/download` only when an operator downloads one archived baseline run report, `POST /api/evaluation/runs` only when an operator clicks `Run evaluation`, `GET /api/evaluation/runs/{runId}/report/download` only when an operator downloads one archived full evaluation report, and `POST /api/github/webhook-diagnostics/evaluate-payload` only when an operator submits pasted webhook diagnostic fields, so Markdown reports, handoff packages, archive records, delivery receipts, readiness snapshots, fixture baseline execution, archived fixture baseline evidence, baseline regression reports, full evaluation run reports, launch readiness archives, launch evidence package archives, launch evidence share reports, and payload diagnostics are created or retrieved from current operator input without adding another task-triggering path. The handoff archive summary call is read-only over stored archives and refreshes after a new handoff package archive is created; the summary panel can copy the already loaded Markdown report or download the same evidence as a backend Markdown attachment. The handoff share checklist, share center, finalization gate, share instructions, self-hosted launch readiness package, launch evidence package, and launch evidence share center are read-only over current or archived evidence and refresh after a new handoff package archive, delivery receipt, or launch evidence package archive changes the underlying evidence. Self-hosted launch readiness archive creation is a local evidence write only: it records the current launch package and protected admin audit evidence, then prepends the archive to the visible history; it does not create tasks, call the model, run tests, mutate Git, send messages, or write to GitHub. Launch evidence package archive creation is also a local evidence write only: it records the current final launch package and protected admin audit evidence, then prepends the archive to the visible history and refreshes the share center; it does not create tasks, call the model, run tests, mutate Git, send messages, or write to GitHub. The launch evidence share center itself is read-only over the latest archived launch package and does not create or refresh archives. Handoff share delivery receipt creation is a local evidence write only: it records channel, target, operator, notes, delivered time, source archive/session ids, and Markdown evidence after instructions are send-ready, then refreshes share-center, finalization, and self-hosted launch readiness state; it does not send messages, create tasks, call the model, mutate Git, or write to GitHub. GET session-report endpoints remain compatible for API clients that do not have browser-local command or outcome context. The webhook URL readiness check is read-only and only probes the configured public `/health` endpoint; it does not edit GitHub webhook settings or redeliver events. The webhook setup readiness summary is also read-only: it combines secret configured state, payload URL readiness, latest delivery status, redelivery recommendation, next actions, and copyable Markdown evidence without exposing the secret or mutating GitHub. The demo readiness panel is read-only until the explicit archive action; readiness snapshot creation stores local evidence only and does not create tasks, call the model, mutate Git, or write to GitHub. Readiness snapshot trend comparison is read-only over archived readiness rows and refreshes after a new readiness snapshot is created. The evaluation case catalog panel is read-only except for the explicit fixture baseline actions and full evaluation run action, which run adapter-selected verification commands against checked-in local fixtures only; they do not create tasks, call the model, mutate Git, or write to GitHub. Baseline regression comparison is read-only over archived baseline rows and refreshes after a new archive is created. Full evaluation run archives combine fixture baseline output with catalog coverage and safety-rejection coverage, store PatchPilot-local Markdown evidence, and do not create tasks, call the model, mutate Git, or write to GitHub. The trigger-decision panel reuses the selected task timeline and rejected-trigger data to compare accepted and rejected decisions in one place. The admin audit trail panel renders the protected mutation stream, supports server-backed action/operator/resource filtering, and copies a Markdown evidence report for the visible rows, while the rejected-trigger panel filters quarantine-related safety audit rows from that stream and posts manual trigger quarantine create and release actions to the same protected quarantine API so operators can block abusive trigger users or repositories and recover false positives without leaving the dashboard.
It also loads `GET /api/task-queue/worker-health` with the queue summary and queue item APIs so operators can see whether the backend worker poller is not started, polling, idle, active, or most recently errored without reading process logs.
The external exposure readiness panel loads `GET /api/security/external-exposure-readiness` and `GET /api/security/external-exposure-readiness/archives` so operators can inspect the current temporary-public-URL safety gate and recent archived gate evidence before sharing a Cloudflare Tunnel URL. It archives only when an operator clicks `Archive`, calling `POST /api/security/external-exposure-readiness/archives`, prepends the returned archive to the visible history, and downloads frozen Markdown evidence through `GET /api/security/external-exposure-readiness/archives/{archiveId}/report/download`. These archive actions write PatchPilot-local evidence only and do not create tasks, call the model, run tests, mutate Git, send messages, write GitHub comments, or expose secrets.
It calls `POST /api/demo/launch-command` only when an operator submits the launch command composer, turning structured repository, issue, operation, target path, and replacement text fields into a copyable `/agent fix` comment and reusable launch-preflight input without creating tasks or mutating GitHub. Successful command compositions are stored only in browser `localStorage` as `patchpilot.demoLaunchCommandHistory`, keeping the five most recent unique commands available for copy, composer refill, launch-preflight reuse, session-report handoff evidence, final launch-package evidence, and post-launch tracking during repeated demo practice. It calls `POST /api/demo/launch-preflight` only when an operator submits the launch preflight form, combining current demo readiness with a read-only GitHub issue-comment trigger dry run for the exact `/agent fix` comment that will be posted. After a launch preflight result exists, the panel can copy a Markdown launch package that combines the exact GitHub issue URL, comment, readiness and trigger-gate evidence, browser-local prepared commands, and next actions without creating another backend record. The demo launch tracker uses the same browser-local prepared commands and correlates them with already-loaded webhook delivery diagnostics, task records, and Pull Request URLs so operators can see whether a posted launch is still waiting for webhook delivery, running, failed, or ready for Pull Request review, then copy a Markdown outcome report for a specific launch without creating another backend record. Operators can also archive recent outcome reports in browser `localStorage` as `patchpilot.demoLaunchOutcomeArchive`, reopen archived task and Pull Request links, copy archived reports again, and clear that local archive. The demo session snapshot reads the same browser-local outcome archive so copied, downloaded, and archived session reports can include both the prepared launch commands and the observed launch outcomes.
The launch evidence package panel also loads `GET /api/demo/launch-acceptance-closeout` as the final launch acceptance section. It displays the accepted/not-accepted state, session/task/Pull Request/webhook/evaluation/archive/final-handoff-archive/receipt identifiers, receipt freshness, check outcomes, evidence notes, next action, and download actions, then downloads the final Markdown report through `GET /api/demo/launch-acceptance-closeout/report/download`. Operators can archive that terminal closeout through `POST /api/demo/launch-acceptance-closeout/archives`, inspect recent closeout archives loaded from `GET /api/demo/launch-acceptance-closeout/archives`, and download historical reports through `GET /api/demo/launch-acceptance-closeout/archives/{archiveId}/report/download`. The same panel loads `GET /api/demo/launch-acceptance-certificate` as the final certified/not-certified readout over the latest closeout archive, repeats the final handoff report package archive proof from the accepted closeout, downloads the certificate through `GET /api/demo/launch-acceptance-certificate/report/download`, archives the current certificate through `POST /api/demo/launch-acceptance-certificate/archives`, lists recent certificate archives from `GET /api/demo/launch-acceptance-certificate/archives`, and downloads archived certificate reports through `GET /api/demo/launch-acceptance-certificate/archives/{archiveId}/report/download`. The current closeout and certificate readouts remain read-only; closeout and certificate archive creation are PatchPilot-local evidence writes only and do not create tasks, call the model, run tests, mutate Git, send messages, record receipts, or write to GitHub.
The final demo acceptance panel loads `GET /api/demo/acceptance-summary` and downloads `GET /api/demo/acceptance-summary/report/download`. It also loads `GET /api/demo/final-acceptance-share-package`, `GET /api/demo/final-acceptance-share-package/archives`, `GET /api/demo/final-acceptance-share-delivery-receipts`, `GET /api/demo/final-acceptance-share-finalization`, `GET /api/demo/final-acceptance-completion-archives`, `GET /api/demo/final-acceptance-completion-evidence-bundle`, `GET /api/demo/final-acceptance-completion-evidence-delivery-receipts`, `GET /api/demo/final-acceptance-completion-evidence-delivery-finalization`, `GET /api/demo/final-acceptance-completion-closeout`, `GET /api/demo/final-acceptance-completion-closeout/archives`, and `GET /api/demo/final-acceptance-share-package/report/download` so the same panel can show the send-ready reviewer package, completion history, final reviewer-facing completion proof, proof that the completion evidence bundle was delivered, whether that delivery closes the current bundle, whether the whole external-review completion loop is closed, and frozen closeout evidence. Operators can archive the current package through `POST /api/demo/final-acceptance-share-package/archives`, download historical reports through `GET /api/demo/final-acceptance-share-package/archives/{archiveId}/report/download`, record delivery evidence through `POST /api/demo/final-acceptance-share-delivery-receipts`, download receipt reports through `GET /api/demo/final-acceptance-share-delivery-receipts/{receiptId}/report/download`, download finalization evidence through `GET /api/demo/final-acceptance-share-finalization/report/download`, archive the READY completion proof through `POST /api/demo/final-acceptance-completion-archives`, download archived completion reports through `GET /api/demo/final-acceptance-completion-archives/{archiveId}/report/download`, download the current completion evidence bundle through `GET /api/demo/final-acceptance-completion-evidence-bundle/report/download`, record bundle delivery through `POST /api/demo/final-acceptance-completion-evidence-delivery-receipts`, download bundle delivery receipt reports through `GET /api/demo/final-acceptance-completion-evidence-delivery-receipts/{receiptId}/report/download`, download the completion delivery finalization report through `GET /api/demo/final-acceptance-completion-evidence-delivery-finalization/report/download`, download the final completion closeout report through `GET /api/demo/final-acceptance-completion-closeout/report/download`, archive the READY and closed final completion closeout through `POST /api/demo/final-acceptance-completion-closeout/archives`, and download frozen closeout reports through `GET /api/demo/final-acceptance-completion-closeout/archives/{archiveId}/report/download`. The panel shows the aggregate accepted/not-accepted state, latest launch certificate archive, latest task evidence certificate archive, checks, evidence notes, share-package recipients, attachments, pre-send checks, message subject/body, copy action, download action, recent archives, finalization status, receipt freshness, receipt recording form, recent receipts, completion archive action, recent completion archives, completion evidence bundle status, bundle evidence notes, bundle download actions, final completion evidence delivery receipt form, recent completion evidence delivery receipts, completion delivery finalization status, final completion closeout status, closeout checks, closeout evidence notes, recent closeout archives, and the no-side-effects contract. Copying the package writes a prepared message to the browser clipboard; downloading retrieves Markdown evidence; archive, receipt, completion archive, completion closeout archive, and completion evidence delivery receipt creation write PatchPilot-local evidence and protected audit only. The completion delivery finalization and live completion closeout views are read-only. None of these actions sends messages, creates tasks, calls the model, mutates Git, or writes to GitHub.

The same panel also loads `GET /api/demo/final-external-review-evidence-package` and downloads `GET /api/demo/final-external-review-evidence-package/report/download` as the final reviewer-facing proof package. It lists recent frozen package archives from `GET /api/demo/final-external-review-evidence-package/archives`, archives the current READY package through `POST /api/demo/final-external-review-evidence-package/archives`, downloads frozen package reports through `GET /api/demo/final-external-review-evidence-package/archives/{archiveId}/report/download`, lists local package delivery receipts from `GET /api/demo/final-external-review-evidence-package/delivery-receipts`, records delivery evidence through `POST /api/demo/final-external-review-evidence-package/delivery-receipts`, downloads receipt reports through `GET /api/demo/final-external-review-evidence-package/delivery-receipts/{receiptId}/report/download`, loads `GET /api/demo/final-external-review-evidence-package/delivery-finalization`, downloads that finalization report through `GET /api/demo/final-external-review-evidence-package/delivery-finalization/report/download`, lists finalization archives through `GET /api/demo/final-external-review-evidence-package/delivery-finalization/archives`, archives the READY finalization through `POST /api/demo/final-external-review-evidence-package/delivery-finalization/archives`, downloads archived finalization reports through `GET /api/demo/final-external-review-evidence-package/delivery-finalization/archives/{archiveId}/report/download`, loads `GET /api/demo/final-external-review-delivery-certificate`, downloads the final certificate through `GET /api/demo/final-external-review-delivery-certificate/report/download`, lists certificate archives through `GET /api/demo/final-external-review-delivery-certificate/archives`, archives the certified certificate through `POST /api/demo/final-external-review-delivery-certificate/archives`, downloads archived certificate reports through `GET /api/demo/final-external-review-delivery-certificate/archives/{archiveId}/report/download`, loads the final release bundle from `GET /api/demo/final-external-review-release-bundle`, downloads that terminal handoff report through `GET /api/demo/final-external-review-release-bundle/report/download`, lists release-bundle archives through `GET /api/demo/final-external-review-release-bundle/archives`, archives the READY release bundle through `POST /api/demo/final-external-review-release-bundle/archives`, downloads archived release-bundle reports through `GET /api/demo/final-external-review-release-bundle/archives/{archiveId}/report/download`, lists release-bundle delivery receipts through `GET /api/demo/final-external-review-release-bundle/delivery-receipts`, records release-bundle delivery evidence through `POST /api/demo/final-external-review-release-bundle/delivery-receipts`, downloads release-bundle delivery receipt reports through `GET /api/demo/final-external-review-release-bundle/delivery-receipts/{receiptId}/report/download`, loads `GET /api/demo/final-external-review-release-bundle/delivery-finalization`, downloads that terminal delivery finalization through `GET /api/demo/final-external-review-release-bundle/delivery-finalization/report/download`, lists terminal delivery finalization archives, loads `GET /api/demo/final-external-review-release-bundle/delivery-certificate`, downloads the terminal release-bundle delivery certificate through `GET /api/demo/final-external-review-release-bundle/delivery-certificate/report/download`, lists release-bundle delivery certificate archives through `GET /api/demo/final-external-review-release-bundle/delivery-certificate/archives`, archives the certified release-bundle delivery certificate through `POST /api/demo/final-external-review-release-bundle/delivery-certificate/archives`, downloads archived release-bundle delivery certificate reports through `GET /api/demo/final-external-review-release-bundle/delivery-certificate/archives/{archiveId}/report/download`, loads the terminal final reviewer handoff package through `GET /api/demo/final-reviewer-handoff-package`, and downloads that package through `GET /api/demo/final-reviewer-handoff-package/report/download`. This readout aggregates the accepted final demo summary, finalized reviewer package, completion evidence bundle, completion delivery finalization, final completion closeout, latest frozen closeout archive, frozen package archive, package delivery receipt, final delivery finalization, frozen delivery finalization archive, final delivery certificate, certified certificate archive, final release bundle, frozen release-bundle archive, release-bundle delivery receipt, release-bundle delivery finalization, release-bundle delivery certificate, frozen release-bundle delivery certificate history, and final reviewer handoff package into one READY/NEEDS_ATTENTION/BLOCKED package with archive ids, delivery proof, finalization checks, certificate checks, release checks, required attachments, evidence notes, download actions, archive history, receipt history, finalization archive history, certificate archive history, release-bundle archive history, release-bundle delivery history, release-bundle delivery finalization history, release-bundle delivery certificate history, reviewer handoff checks, and a no-side-effects contract. Archive and receipt creation are PatchPilot-local evidence writes only: they do not send messages, create tasks, call the model, run tests, mutate Git, or write to GitHub. The live finalization, certificate, release bundle, release-bundle delivery finalization, release-bundle delivery certificate, and final reviewer handoff package views are read-only; finalization archive, certificate archive, release-bundle archive, release-bundle delivery finalization archive, and release-bundle delivery certificate archive creation write only local frozen evidence records. It refreshes after completion archive, completion delivery receipt, completion closeout archive, final package archive, final package delivery receipt, finalization archive, certificate archive, release-bundle archive, release-bundle delivery receipt, release-bundle delivery finalization archive, or release-bundle delivery certificate archive actions so the downloadable final external-review package, latest archive evidence, latest delivery proof, final delivery finalization, frozen delivery closure, certificate, certificate history, release bundle, frozen terminal release evidence, terminal release delivery proof, release-bundle delivery certificate, frozen terminal certificate history, and final reviewer handoff package reflect the latest evidence.

The demo evidence bundle panel also renders `finalExternalReviewEvidencePackage`, `finalExternalReviewEvidencePackageArchiveEvidence`, `finalExternalReviewEvidencePackageDeliveryReceiptEvidence`, `finalExternalReviewEvidencePackageDeliveryFinalization`, `finalExternalReviewEvidencePackageDeliveryFinalizationArchiveEvidence`, `finalExternalReviewReleaseBundle`, `finalExternalReviewReleaseBundleArchiveEvidence`, and `finalExternalReviewReleaseBundleDeliveryFinalization` from `GET /api/demo/evidence-bundle`, repeating the reviewer package readiness, latest package archive, latest package delivery receipt, final delivery finalization, frozen delivery finalization archive, release-bundle readiness, frozen release-bundle archive readiness, terminal release delivery finalization, certificate archive, required attachments, closeout archive, completion archive, completion delivery receipt, task, Pull Request, freshness, delivery target/channel, archived time, next action, and download actions next to the copied runbook source. Older bundle responses that do not include those fields show explicit missing-package, missing-archive, missing-delivery, missing-finalization, missing-finalization-archive, missing-release-bundle, missing-release-bundle-archive, and missing-release-bundle-delivery-finalization guidance instead of hiding the final review proof section.

Selected-task detail uses the `/tasks/{taskId}` frontend route and `GET /api/tasks/{taskId}/detail`, an aggregate read-model endpoint that returns the task audit summary, latest queue item, queue history, accepted-trigger intent audit, latest generated diff, timeline events, test runs, tool calls, model calls, and failed-task diagnosis together. For accepted tasks, the trigger intent audit shows the safety-gate result, issue-context load status, and model trigger-classification outcome as structured fields before the operator scans raw timeline events. For failed and cancelled tasks, the page also loads `GET /api/tasks/{taskId}/retry-preflight` so retry eligibility, stable category, sanitized reason, and next operator action are visible before an operator queues another attempt. The task summary includes selected adapter metadata and a nullable detection reason, which the detail evidence strip renders next to the verification command so operators can see why the task chose that language/build path. A dedicated adapter execution evidence section repeats the selected language/build system, allowlisted verification command, detection reason, next action, and safe-command boundary for supported tasks; shows a pending state before preflight records adapter metadata; and lists supported adapter options when execution stopped as unsupported before model generation, verification, Git mutation, push, or Pull Request creation. If the generated-diff risk gate rejects a patch, the task appears as `PENDING_REVIEW`, the same evidence strip shows a `Risk gate BLOCKED` marker, the generated-diff panel shows the exact patch to inspect before approval, and the tool-call list shows the concrete rejection reason. Operators can cancel pending-review tasks or approve the existing generated diff with an approver selected from the configured review-approval allowlist plus a reason. Approval requeues the task to resume the same workspace after the risk gate instead of asking the model to produce a new patch, and approved tasks show the stored approval audit metadata in detail. If no review approvers are configured, the approval form is disabled and explains the missing configuration. Retry remains hidden for pending-review tasks because retry is reserved for failed or cancelled fresh runs. Failed and cancelled tasks show a `Retry preflight` section; retry remains enabled for categories that can be safely requeued and is disabled as `Retry blocked` when GitHub permissions or repository support must be fixed first. Retry requires an operator-entered reason before the button is enabled, and retried tasks show that reason with the previous status, failure reason, and retry time in retry lineage. Failed tasks with a stored status-comment URL label that GitHub link as `Failure feedback`, because the issue-facing comment includes the failure category, next action, and sanitized reason. Failed tasks also show a `Failure diagnosis` section with the same stable category, next action, and sanitized reason before raw evidence. Pending-review tasks label the same link as `Review feedback`; other tasks keep `Status Comment`. Legacy `?taskId=` links still select the same task. Status, search, repository owner/name filters, adapter filters, created time filters, and non-default sort state are stored as URL query parameters, so `/tasks/{taskId}?status=PENDING_REVIEW&query=maven&repositoryOwner=bingqin2&repositoryName=PatchPilot&language=node&buildSystem=npm&sort=createdAtAsc&createdAfter=2026-06-20T01:00:00Z&createdBefore=2026-06-21T01:00:00Z` restores the selected task and task-list view together. The clear-filter action removes `status`, `query`, `repositoryOwner`, `repositoryName`, `language`, `buildSystem`, `createdAfter`, and `createdBefore`, preserving sort, the selected task route, unrelated query parameters, and hash fragments. Markdown task reports use `GET /api/tasks/{taskId}/report` so operators can copy a compact diagnostic summary, trigger intent audit, adapter execution evidence, failure diagnosis, retry reason, and generated diff without manually assembling API responses. This keeps the dashboard detail panel focused on the selected task while preserving narrower backend endpoints for curl-based debugging.

Reusable dashboard components live under `frontend/src/dashboard/components/`:

- `TaskListPanel`: task list, status filters with scoped count badges, repository owner/name filters, language/build-system adapter filters, created time range filters, backend-backed search, backend-backed newest/oldest sort control, clear-filter action, pagination counts, issue/status/PR links.
- `TaskDetailPanel`: selected task summary, copyable task deep link, copyable task report, queue state and queue history, execution evidence strip, adapter execution evidence, accepted-trigger intent audit, failed-task diagnosis, retry preflight with required retry reason, generated-diff preview, configured-approver approval form, approval audit metadata, retry lineage, timeline, test runs, tool calls, model calls, cancel/retry/approve-review actions.
- `ConnectivityPanel`: top-of-page operational check for backend `/health`, saved browser admin-token state, protected API reachability, and the next corrective action when startup, proxy, or admin-token access is wrong.
- `OperatorSetupChecklistPanel`: read-only demo setup checklist that turns connectivity, required credentials, GitHub credential readiness, public webhook URL reachability, selected repository access readiness, safety policy, demo target policy alignment, adapter fixtures, queue health, worker heartbeat readiness, and recent Pull Request evidence into ready/attention checks with next actions.
- `SelfHostedLaunchReadinessPanel`: final pre-launch readout that combines demo readiness, evidence bundle status, handoff finalization, credential, webhook setup, queue/worker checks, next actions, current Markdown report download, local archive action, recent archive rows, and archived report downloads without creating tasks.
- `DemoLaunchEvidencePackagePanel`: final shareable evidence artifact that combines self-hosted readiness, session id, live task/Pull Request/webhook proof, evaluation coverage, post-demo proof, final handoff report package archive proof, next actions, copyable Markdown, downloadable Markdown, local package archive action, recent archived launch evidence rows, archived report downloads, a launch share center, a launch finalization gate, local launch delivery receipt recording and receipt downloads, launch acceptance closeout archives, a final launch acceptance certificate, certificate archives, and a read-only side-effect contract without creating tasks.
- `DemoAcceptanceSummaryPanel`: final demo acceptance view that combines the latest launch acceptance certificate archive and task evidence acceptance certificate archive into one accepted/not-accepted summary, reviewer-facing share package, local final acceptance delivery receipt recording, finalization gate, final completion archive history, completion evidence bundle, completion delivery finalization gate, final completion closeout, final external-review package archive controls, final external-review package delivery receipt recording, final delivery finalization, final delivery certificate, certificate archive controls, final release-bundle archive controls, release-bundle delivery receipt recording, terminal release delivery finalization, terminal delivery finalization archive controls/history, terminal delivery certificate archive controls/history, recent receipt/archive history, and Markdown downloads without sending external messages.
- `DemoEvidenceBundlePanel`: top-level demo evidence summary that combines readiness, smoke checklist, adapter fixture counts, queue state, recent task/PR evidence, latest webhook delivery, rejected-trigger counts, active quarantine count, handoff share checklist status, final handoff share-center status and download actions, latest handoff share delivery receipt summary and freshness state, handoff finalization status, latest launch acceptance closeout archive evidence, latest launch acceptance certificate archive evidence, final acceptance delivery/finalization evidence, final acceptance completion closeout evidence, final external-review package evidence, final external-review package archive evidence, final external-review package delivery receipt evidence, final external-review package delivery finalization evidence, final external-review release bundle evidence, final release-bundle archive evidence, final release-bundle delivery finalization evidence, final release-bundle delivery finalization archive evidence, final release-bundle delivery certificate archive evidence, and a copyable Markdown runbook.
- `DemoSessionSnapshotPanel`: one-panel demo session summary that combines current evidence, readiness snapshot trend status and deltas, backend-provided structured handoff readiness checks, prepared launch commands from browser-local history, archived launch outcomes from browser-local outcome archive, script step count, runbook-backed handoff context, operator checklist, health contract, share summary, next actions, copy/download Markdown session report actions, copy/download Markdown handoff package actions, session and handoff package archive actions, copy/download handoff archive summary evidence, copy/download handoff share instructions, final handoff share-center delivery summary with fresh/stale/missing receipt status, a final handoff finalization gate with report download, local handoff share delivery receipt recording and receipt downloads, copyable handoff share checklist evidence, and recent archived session reports and handoff packages with copy/download actions.
- `DemoScriptPanel`: ordered live-demo script with operator actions, verification commands, success criteria, troubleshooting panels, current evidence, and a read-only health contract.
- `DemoReadinessPanel`: first-screen readiness gate that summarizes whether a controlled issue-to-PR demo is ready, needs attention, or is blocked, including visible per-check operator actions, recent readiness snapshots, snapshot trend movement, and copy/download Markdown evidence actions.
- `DemoSmokeChecklistPanel`: final pre-demo checklist that orders readiness gate, webhook delivery, task execution, and Pull Request evidence with concrete next actions.
- `DemoLaunchCommandPanel`: structured command composer that generates a controlled `/agent fix` comment for the demo issue, exposes the GitHub issue link, copies the generated command, saves browser-local recent command history, refills the composer from saved commands, and applies returned preflight input to the launch preflight form without creating a task.
- `DemoLaunchPreflightPanel`: final launch gate that submits the exact repository, issue, trigger user, and GitHub issue comment through `POST /api/demo/launch-preflight`, then shows ready/blocked status, trigger evaluation evidence, issue-context state, blocked reason, next actions, a copyable Markdown preflight report, and a copyable launch package with browser-local prepared command evidence without creating a task.
- `DemoLaunchTrackerPanel`: read-only post-launch tracker that matches browser-local prepared commands to recent webhook deliveries, task rows, and Pull Request URLs, then shows webhook, task, PR, task-link, PR-link, next-action evidence, a copyable Markdown outcome report, and a browser-local outcome archive without new backend calls.
- `ManualTaskForm`: local demo/debug trigger evaluation through `POST /api/tasks/evaluate-trigger` plus task creation through `POST /api/tasks`, with explicit repository, issue, trigger user, `/agent fix` command fields, a preview-source selector for manual API versus GitHub issue-comment gate behavior, and a copyable Markdown evaluation report for accepted or blocked dry-runs.
- `QueuePanel`: read-only queue health, worker heartbeat readiness, last poll age, operator action, summary, and queue items.
- `TriggerDecisionPanel`: selected accepted-trigger evidence beside recent rejected trigger decisions, refusal-comment links, and category summary for quick operator comparison.
- `AcceptedTriggerDecisionPanel`: recent accepted `/agent fix` trigger decisions from persisted pre-execution records, including repository, issue, task status, command, safety, active-task, quarantine, rate-limit, model intent, issue-context state, timestamp, and a task-opening action.
- `RejectedTriggerPanel`: recent `/agent fix` attempts that were refused before task creation, including command text, repository, trigger user, delivery id, stable rejection category, rejection reason, retry eligibility guidance, a category filter, and a compact recent-window summary by category, source, top trigger users, and top repositories. It also surfaces active trigger-user and repository quarantine records with evidence counts, expiry times, creator metadata, manual create controls, release buttons, an evidence drilldown for one selected quarantine, recent operator safety audit rows, and `ABUSE_QUARANTINED` audit rows created by repeated rejected-trigger patterns.
- `WebhookDeliveryPanel`: read-only recent delivery diagnostics for ignored comments, rejected triggers, duplicate deliveries, invalid signatures, bad payloads, active-task collisions, and task-created outcomes, including whether a delivery should be fixed and redelivered from GitHub. Each row renders the correlated outcome target, such as a task detail link, rejected-trigger audit anchor, ignored outcome, duplicate outcome, or error outcome. It also provides a pasted-payload diagnostic form backed by `POST /api/github/webhook-diagnostics/evaluate-payload` so operators can check payload JSON, signature status, event/action support, `/agent fix` recognition, parsed target fields, and the next action before redelivery.
- `ConfigurationPanel`: read-only runtime configuration summary with backend health, provider, model, workspace, queue policy, queue worker heartbeat stale threshold, trigger rate limits, rejected-trigger quarantine policy, generated-diff policy state, configured/missing secret states, and setup health hints.
- `AdapterReadinessReportPanel`: read-only adapter readiness summary that combines supported languages, allowlisted verification commands, fixture pass rate, fixture failures, adapter API warnings, and a copyable Markdown report.
- `EvaluationCaseCatalogPanel`: read-only benchmark map that summarizes readiness status, supported fix cases, safety rejection cases, language/build-system coverage, rejection categories, next action, expected verification commands, expected changed files, success criteria, safety expectations, a benchmark-shaped run preview, recent archived fixture baseline runs, recent archived full evaluation runs, baseline regression status, pass/fail/skip deltas, newly failed cases, recovered cases, known gaps, side-effect contract, and copyable/downloadable Markdown reports.
- `SupportedAdaptersPanel`: read-only support matrix for Java/Maven, Java/Gradle, Go, Node/Bun, Node/npm, Node/pnpm, Node/yarn, Python/tox, Python/nox, Python/hatch, Python/Poetry, Python/uv, and Python/pytest adapters.
- `RepositoryPreflightPanel`: local diagnostic form that runs adapter detection against a backend-local repository path before an operator creates a real task. It shows supported status, selected adapter metadata, verification command, detection reason, unsupported adapter guidance, configured allowed roots, and a copyable Markdown preflight report without running tests or mutating Git.
- `AdapterFixtureVerificationPanel`: read-only fixture verification matrix showing expected versus actual adapter detection for each checked-in demo fixture.
- `FailureCausePanel`, `ModelUsagePanel`, and `LatencyPanel`: operational summary cards for failure grouping, token usage, call counts, estimated model cost, and execution latency. Failure grouping uses the same stable categories and next-action guidance as failed-task issue feedback.
- `MetricCard`, `RecordLine`, and `SummaryItem`: small shared presentation units.

Formatting helpers live in `frontend/src/dashboard/format.ts`.

## Current UX Model

The first screen is the working dashboard:

- Demo readiness summarizes credentials, public webhook URL reachability, safety policy state, demo target policy alignment, adapter fixture verification, adapter runtime executables, evaluation baseline regression evidence, queue health, worker heartbeat readiness, recent Pull Request evidence, and recent readiness snapshot trend movement before an operator starts a live smoke run.
- Self-hosted launch readiness gives one final status, summary, check list, next-action list, Markdown download, archive action, and recent archive list that ties together readiness, evidence bundle, handoff finalization, credentials, webhook setup, and queue/worker state before an operator posts a live trigger or shares demo evidence.
- The demo launch evidence package gives one final shareable status, summary, session id, live-run proof, evaluation coverage, post-demo proof, next actions, copy action, download action, local archive action, recent archive history, archived report downloads, a latest-archive share center with receipt freshness, a finalization gate that requires a fresh receipt for the current archive/session, local launch delivery receipt recording, receipt report downloads, and read-only contract for handoff or review without creating tasks.
- The demo evidence bundle gives one status, count set, latest evidence links, full evaluation run readiness, coverage and safety-category evidence, final handoff share-center send/no-send evidence, final launch share-center send/no-send evidence, launch, handoff, and final acceptance finalization evidence, latest delivery receipt evidence with fresh/stale/missing status, and next-action list for live-demo readiness.
- The demo session snapshot gives one session id, share summary, readiness trend status and deltas, prepared launch commands, archived launch outcomes, checklist, health contract, current evidence, script context, copyable and downloadable Markdown session reports, copyable and downloadable Markdown handoff packages, a share-ready handoff archive summary with copy/download evidence actions, a final share center, a finalization gate that requires share readiness plus a fresh receipt, final share instructions with recipients, attachments, checks, and a message template, local delivery receipts that prove where and when the package was sent externally and whether the latest receipt matches the current archive/session, a share checklist that says whether the latest archived package is ready to send, and recent archived session reports and handoff packages for pre-run confirmation or post-run handoff.
- The demo script turns that evidence into a guided path with concrete commands, success criteria, troubleshooting panels, and a read-only contract before the operator touches GitHub.
- The smoke checklist turns readiness, webhook delivery, task execution, and Pull Request evidence into ordered final checks before the operator posts the live `/agent fix` comment.
- The demo launch command composer turns structured fields into a validated `/agent fix` comment, exposes the target GitHub issue, supports copying the command, stores the five most recent commands in local browser history, can refill the composer from that history, and can fill the launch preflight form directly.
- The demo launch preflight lets the operator paste the exact issue comment planned for GitHub, confirms whether current readiness plus issue-comment task-creation gates allow it, and copies either the raw preflight evidence or a full launch package with issue URL, comment, prepared command context, and next actions before the live comment is posted.
- The demo launch tracker turns those prepared commands into post-launch rows by matching them to recent webhook, task, and Pull Request evidence so the operator can tell whether to wait, inspect the task, troubleshoot webhook delivery, or review the Pull Request, then copy a compact outcome report for handoff or save it to the local outcome archive.
- Metrics summarize task health.
- A compact refresh status tells operators when top-level dashboard data is still loading, and the title area shows when the dashboard last refreshed successfully.
- Operational summaries highlight failure causes, model usage, and latency without requiring terminal inspection. Failure causes show stable categories such as `VERIFICATION_FAILED` and `GITHUB_OPERATION_FAILED` with the next action returned by the backend metrics API.
- A manual task form can evaluate a local demo/debug `/agent fix` trigger before creation, switch the dry-run source between manual API and GitHub issue-comment gate behavior, copy a Markdown evidence report for the dry-run decision, then enqueue the task through the same backend task and queue path used by webhook-created tasks.
- The task list supports URL-backed status filters with scoped count badges, repository owner/name filters, language/build-system adapter filters, created time range filters, backend-backed search over task history, newest/oldest sorting, one-click filter reset, loaded-versus-total counts, and incremental loading.
- Status count badges come from `GET /api/tasks/status-counts`. They follow search, repository, adapter, and created-time filters, but ignore the active status, sort, limit, and offset so operators can see the distribution before switching status tabs.
- Metrics and operational summary panels use the same search, repository, adapter, and created-time scope as the task list, so a filtered Node/npm view does not show global Maven/Python task totals.
- Selecting a task updates the `/tasks/{taskId}` route while preserving active filter query parameters, loads aggregate task detail in one request, and reveals a copyable task link, copyable Markdown report, selected language adapter metadata with detection reason, accepted-trigger intent audit when present, failed-task diagnosis when present, queue state, queue history, execution evidence summary, timeline events, verification output, tool-call records, model-call records, and GitHub links.
- Generated-diff risk-gate failures are visible before scrolling through raw records. The generated-diff preview shows the exact patch before approval, with `GeneratedDiffRiskGate` still available in tool-call evidence for exact rejection details.
- Pending-review tasks expose an explicit approve-review form that records an approver and reason before resuming the already-generated workspace.
- Queue visibility shows whether work is pending, delayed, running, failed, or cancelled, with failed/delayed/running health hints before the row list. The same panel shows worker heartbeat state, readiness status, last poll age, operator action, poll count, claimed count, the latest claimed task id, and the latest worker error so operators can distinguish queued work from an inactive, stale, or failing poller.
- Rejected-trigger visibility shows why a `/agent fix` comment did not create a task when the safety gate, allowlists, rate limiter, rejected-trigger quarantine, or model classifier intentionally refused it. Each row shows a category badge, and the panel can filter by category through the URL-backed `rejectedCategory` parameter so operators can isolate vague requests, dangerous instructions, unauthorized users, blocked repositories, rate limits, abuse quarantine, and model-classifier refusals without parsing reason text. Rows also expose retry eligibility before an operator acts: direct retry remains enabled only for safe actionability/model-classification cases, while dangerous, unauthorized, rate-limited, quarantined, unsupported, unknown, or already-retried rows show `Retry blocked` with the next action. The panel also summarizes the recent rejection window by category, source, top trigger users, and top repositories; category summary rows are clickable and reuse the same filter. Active trigger quarantines are shown above the audit rows so operators can see the current enforced safety state before reading individual refusals, manually add a temporary trigger-user or repository block, inspect the evidence behind a selected quarantine, and release a false-positive quarantine. Recent operator safety audit rows are shown in the same panel so manual create/release actions are traceable by operator, reason, target, and time.
- Admin audit visibility shows protected operator mutations with server-backed action, operator, resource type, resource id, and scope-key filters. The panel can copy the current filtered rows as Markdown evidence for a demo handoff, incident note, or pull-request review context.
- Webhook delivery visibility shows whether the webhook setup is ready, blocked, or needs redelivery attention, then shows whether recent GitHub deliveries reached PatchPilot, why they were ignored or rejected, and which task id was created when a delivery succeeded. The operator setup checklist and live smoke checklist both prefer the combined webhook setup gate over URL-only readiness, so a missing secret, unreachable public payload URL, or redelivery-required latest delivery is visible before an operator posts the live trigger. Pasted payload diagnostics let an operator validate a raw GitHub delivery payload and optional signature without creating task, queue, rejection, delivery, or GitHub comment records.
- Trigger decision visibility shows why the selected `/agent` comment was accepted and what recent comments were refused instead, using the selected task's `TRIGGER_ACCEPTED` timeline event plus recent rejected-trigger rows. When a rejected row has a GitHub refusal comment URL, the quick comparison view includes a `Refusal comment` link so an operator can open the issue-facing explanation without leaving the trigger-decision context.
- Manual trigger evaluation visibility shows whether a proposed local `/agent fix` would create a task or be blocked, which source was evaluated, which gate made that decision, whether issue context was loaded for model classification, and the next operator action. The copy action exports the evaluated repository, issue, trigger user, command, status, blocked reason/category, per-gate decisions, issue-context state, and next action as Markdown for demos, issue comments, or review notes. It is read-only and does not create tasks, queue items, rejected-trigger audit rows, GitHub comments, webhook delivery diagnostics, or rate-limit records.
- Trigger intent visibility in task detail turns the accepted-trigger timeline evidence into safety, issue-context, and model-decision fields so copied reports and per-task investigation show why execution proceeded.
- Configuration visibility shows backend `/health` status, the active provider, model, workspace root, queue policy, worker heartbeat stale threshold, trigger rate-limit thresholds, rejected-trigger quarantine thresholds, generated-diff risk-gate state, protected path pattern count, repository-preflight allowed roots, whether required secrets are configured, and clear health hints for missing secrets or weak optional settings without exposing secret values.
- Supported-adapter visibility shows each supported language/build system, verification command, detection signals, and demo fixture path. Adapter readiness visibility summarizes the same data with fixture pass rate, language coverage, allowlisted commands, runtime executable availability, fixture failures, and copyable Markdown evidence. Fixture verification visibility shows whether each demo fixture still maps to the expected adapter and command. If any adapter API fails, the readiness and detailed panels show local warnings while the rest of the dashboard can still load.
- Evaluation case catalog visibility shows the first benchmark map for Java/Maven, Node/npm, Python/pytest, Go, unsafe-trigger rejection, and vague-trigger rejection cases. It gives operators readiness status, fixture-readiness status, full evaluation run readiness, next action, expected commands, changed files, missing fixture files, adapter drift, success criteria, safety expectations, a run preview report with known gaps, recent archived fixture baseline runs, and recent archived full evaluation runs. Operators can copy static benchmark evidence without executing cases, explicitly run and archive the checked-in fixture baseline, or run and archive a full local evaluation report when they need executable local evidence plus safety coverage; the full evaluation readiness summary refreshes after a run and can be copied as Markdown evidence.
- Repository preflight visibility lets an operator enter an allowed local path such as `docs/demo-repositories/java-maven`, see the configured allowed roots, run the real adapter registry, and see whether that repository shape would be supported before creating a task. After a result exists, the operator can copy a Markdown preflight report with status, repository path, selected adapter, verification command, detection reason, operator action, allowed roots, and supported adapter options for unsupported repositories.
- If the readiness API fails, the demo readiness panel shows a local warning while the rest of the dashboard can still load.
- Cancel and retry are available only for task states where those actions make sense; failed and cancelled tasks show retry preflight before queuing another attempt, require an operator reason before retrying, and retried tasks keep source failure context plus the retry reason in the detail panel.

This keeps the UI focused on operator questions:

- What is running right now?
- Is the queue worker alive, idle, active, or failing?
- Is the queue worker fresh enough for a live smoke run?
- What failed and why?
- Did tests pass?
- Which language adapter and verification command ran?
- Why was that language adapter selected?
- Which repository shapes are supported right now?
- Are all adapter fixtures still ready for a multi-language demo?
- Do the checked-in demo fixtures still detect as the expected adapters?
- Can I copy evidence that a specific local repository path is supported before posting `/agent fix`?
- Why did this `/agent fix` comment not create a task?
- Why was this `/agent fix` comment accepted while similar recent comments were rejected?
- What did the agent call?
- How many model tokens were used?
- Where is the GitHub issue or Pull Request?
- Did GitHub's latest webhook delivery reach PatchPilot and, if not, was it a signature, payload, safety, duplicate, or active-task issue?
- Is there enough live-demo evidence to proceed, or should setup be fixed first?
- Can I share a URL that reopens the same task detail?
- Can I copy a compact report that explains the selected task state?

## Final Target

The long-term frontend target is a complete maintainer dashboard for safe issue-to-PR automation.

Target capabilities:

- Search and filter complete task history.
- Inspect task timeline, generated diff, model calls, tool calls, command runs, and test records.
- Track queue health, retries, cancellation, and worker recovery.
- Surface cost, latency, success rate, failure rate, and test pass rate.
- Link every important UI object back to GitHub.
- Provide safe operator controls such as cancel, retry, generated-diff review approval, and configuration inspection.
- Make safety decisions observable by showing rejected trigger audit records, active trigger quarantines, operator safety audit records, and recent rejection-pattern summaries in the same control plane as accepted tasks.
- Prevent unsafe retry actions by showing rejected-trigger retry eligibility and blocked reasons before creating any task.
- Prevent blind task retries by showing task retry preflight, requiring an operator retry reason, and disabling retry when setup or repository support work is required first.
- Make accepted and rejected trigger decisions comparable from the selected task view before an operator drills into full audit or quarantine controls.
- Make accepted trigger decisions globally auditable so operators can confirm which recent `/agent fix` comments were allowed, which gates passed, and which task each decision created.
- Let operators dry-run a manual `/agent fix` trigger before task creation, using the same backend gates without mutating task, queue, GitHub, rejected-trigger, or rate-limit state, and copy the dry-run gate evidence for review or demo handoff.
- Let operators compose a controlled demo `/agent fix` comment from structured inputs, copy it, and apply it to launch preflight without creating a task or mutating GitHub.
- Let operators reuse recently composed demo `/agent fix` comments from browser-local history during repeated smoke runs without creating tasks, mutating GitHub, or depending on terminal notes.
- Let operators run a final demo launch preflight for the exact GitHub issue comment that will be posted, combining readiness and trigger-gate evidence before the live webhook path starts.
- Let operators copy a final demo launch package after preflight so the issue URL, exact comment, gate evidence, prepared command history, and next actions are preserved before posting on GitHub.
- Let operators track the posted demo launch from prepared command to webhook delivery, task state, and Pull Request review link without manually comparing multiple panels.
- Let operators copy a Markdown outcome report for a tracked demo launch so the exact command, webhook result, task result, Pull Request URL, and next action survive after the live run.
- Let operators keep a browser-local archive of recent demo launch outcome reports so repeated smoke runs remain reviewable after refresh without adding backend state.
- Let operators copy or download a Markdown handoff package that summarizes session evidence, webhook delivery readiness, readiness trend evidence, prepared command counts, archived outcome counts, recent task and Pull Request evidence, next actions, and the embedded session report.
- Let operators archive that final handoff package separately from session reports so the post-demo artifact can be reopened, copied, downloaded, and summarized as share-ready or still requiring action later.
- Let operators copy a final handoff share checklist that converts the latest archived handoff package into concrete share-readiness checks before they send the post-demo evidence.
- Let operators see the final handoff share-center status and download actions in the top-level demo evidence bundle before opening the full session snapshot.
- Let operators see and download a final handoff finalization report that only passes after the package is share-ready and the latest delivery receipt is fresh for the current archive/session.
- Let operators copy or download final handoff share instructions with recipients, attachments, pre-send checks, and a message template while keeping actual delivery outside PatchPilot.
- Let operators record local handoff share delivery receipts after external delivery and download receipt Markdown so post-demo evidence includes who sent what, where, and when without PatchPilot sending messages.
- Let operators inspect, download, archive, reopen, and download recent final self-hosted launch readiness packages before the live trigger or post-demo handoff, using the same backend evidence as the dashboard instead of manually comparing multiple panels.
- Let operators inspect and download a launch evidence share center that turns the latest archived final launch package and latest delivery receipt into final share/no-share status, concrete download actions, archive/task/Pull Request/webhook/evaluation evidence, receipt freshness evidence, and a Markdown report.
- Let operators record local launch evidence delivery receipts after external delivery and download receipt Markdown so final launch evidence includes who sent what, where, and when without PatchPilot sending messages.
- Let operators see and download a final launch evidence finalization report that only passes after the archived launch evidence package is share-ready and the latest delivery receipt is fresh for the current archive/session.
- Let operators see and download one final launch acceptance closeout report that confirms self-hosted launch readiness, current launch evidence, share-center delivery proof, and launch finalization all agree before treating the demo evidence as complete.
- Let operators archive, reopen, and download recent final launch acceptance closeout reports so the final accepted/not-accepted decision survives refreshes and database-backed backend restarts, and repeat the latest closeout archive status, accepted flag, linked launch evidence archive, delivery receipt, next action, and download actions in the top-level demo evidence bundle.
- Let operators see, archive, reopen, and download recent final launch acceptance certificates so only the latest accepted READY closeout archive can be used as the certified external-review record and that certificate survives refreshes and backend restarts. Repeat the latest certificate archive status, certified flag, linked closeout archive, linked launch evidence archive, delivery receipt, next action, and download actions in the top-level demo evidence bundle and copied runbook.
- Let operators copy, download, archive, reopen, and download recent final demo acceptance share packages that convert the accepted launch and task certificate evidence into reviewer-facing recipients, required attachments, pre-send checks, and prepared message text without sending anything externally.
- Let operators see final acceptance delivery/finalization evidence in the top-level demo evidence bundle and copied runbook, including the current archive id, task id, delivery receipt id, target/channel, receipt freshness, summary, and next action before opening the final acceptance panel.
- Let operators archive, reopen, and download final acceptance completion records once the reviewer-facing package finalization is READY so the external-review handoff completion proof remains available after the live finalization readout changes.
- Let operators download a final acceptance completion evidence bundle that summarizes the latest finalization and completion archive as one reviewer-facing proof package.
- Let operators see and download a final acceptance completion delivery finalization report that only passes when the latest completion evidence delivery receipt matches the current completion evidence bundle.
- Let operators see and download one final acceptance completion closeout report that confirms final demo acceptance, reviewer-package finalization, completion evidence bundle, completion archive, completion delivery receipt, and delivery finalization all agree before treating the external-review completion loop as closed. Repeat that closeout status, closed flag, task/Pull Request/archive/receipt identifiers, freshness, next action, and download actions in the top-level demo evidence bundle and copied runbook.
- Let operators archive and download final acceptance completion closeout records so the exact READY/closed closeout proof remains available after live readiness evidence changes.
- Let operators see the latest final acceptance completion closeout archive directly in the top-level evidence bundle and copied runbook, including archive status, closed flag, linked completion archive, delivery receipt, task, Pull Request, archived time, and download actions.
- Let operators archive, reopen, and download final external-review evidence package records so the exact reviewer-facing package remains available after live readiness evidence changes, and repeat the latest archive in the top-level demo evidence bundle and copied runbook.
- Let operators see and download a final external-review package delivery finalization report that only passes when the latest package delivery receipt matches the current frozen final external-review package archive, freeze that READY finalization as a durable archive, reopen/download recent finalization archives, and repeat the latest archive in the top-level demo evidence bundle and copied runbook.
- Let operators see, archive, reopen, and download a final external-review delivery certificate that certifies the latest archived delivery finalization as the terminal reviewer-facing delivery proof without mutating runtime or GitHub state.
- Make accepted-trigger intent available inside task detail and copied reports so task-level investigation explains why execution was allowed before showing what the agent did.
- Show the supported adapter matrix, fixture paths, and fixture pass/fail verification for demo planning and troubleshooting.
- Show the evaluation case catalog, fixture-readiness report, archived fixture baseline runs, archived full evaluation runs, and full evaluation readiness summary as the first evaluation-harness surface, proving which language and safety scenarios PatchPilot intends to measure, whether the checked-in fixtures still support that claim, and whether the latest combined fixture-safety evaluation archive is usable as demo readiness evidence before hosted model benchmark runs are added.
- Support manual demo trigger evaluation and task creation while keeping creation on the durable backend queue.
- Support demo readiness by making a full task lifecycle understandable without reading logs.
- Support a read-only demo script that gives operators the next action and verification command without adding another task-triggering path.
- Support final smoke-check decisions by pairing every live-demo step with current evidence and a next action.
- Support demo session archiving so operators can keep a recent record of the exact Markdown report used during a live demo, with database-backed local profiles preserving those records across backend restarts.
- Include readiness trend evidence, browser-local prepared demo launch commands, and archived demo launch outcomes in copied, downloaded, and archived session reports so handoff records show whether demo readiness improved or regressed, which controlled GitHub issue comments were prepared, and what happened after they were posted.

## Future Work

Additional future work:

- Worker and queue health panels.
- Configuration editing for local setup after the read-only health model is stable.

## Non-Goals

The frontend should not:

- Replace GitHub issue comments as the primary production task trigger.
- Auto-merge Pull Requests.
- Replace GitHub's review UI.
- Hide task execution behind a chat-only interface.
- Become a marketing landing page.
- Execute arbitrary commands from the browser.

## Success Criteria

The frontend is successful when a maintainer can open it during a live demo or local test run and answer these questions without terminal access:

- Which task was created from which GitHub issue?
- What is its current status?
- What did the agent do?
- Which tools and model calls ran?
- Did verification pass?
- If it failed, what was the failure reason?
- If it succeeded, where is the Pull Request?
