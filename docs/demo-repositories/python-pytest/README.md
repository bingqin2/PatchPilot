# Python pytest Demo

Adapter: Python/pytest

Verification command: `python3 -m pytest`

This fixture contains `pytest.ini`, a tiny Python module, and a pytest test. PatchPilot should detect it through the Python/pytest adapter.
