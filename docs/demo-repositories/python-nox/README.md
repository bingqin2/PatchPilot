# Python Nox Demo Repository

Expected adapter: `python/nox`

Expected verification command:

```bash
nox
```

This fixture represents a Python repository that uses `noxfile.py` as the project-level test runner.
