# Java Gradle Demo

Adapter: Java/Gradle

Verification command: `gradle test`

This fixture contains `build.gradle`, a tiny Java class, and a JUnit test. PatchPilot should detect it through the Java/Gradle adapter.
