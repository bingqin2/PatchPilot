# Node Bun Demo

Adapter: Node/Bun

Verification command: `bun test`

This fixture contains `package.json`, `bun.lockb`, a tiny JavaScript module, and a Bun-compatible test script. PatchPilot should detect it through the Node/Bun adapter before the broader Node/npm adapter.
