# Java Maven Demo

Adapter: Java/Maven

Verification command: `mvn test`

This fixture contains `pom.xml`, a tiny Java class, and a JUnit test. PatchPilot should detect it through the Java/Maven adapter.
