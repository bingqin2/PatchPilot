# Python Hatch Demo Repository

Expected adapter: `python/hatch`

Expected verification command:

```bash
hatch test
```

This fixture represents a Python repository that exposes a Hatch test script in `pyproject.toml`.
