# Node pnpm Demo

Adapter: Node/pnpm

Verification command: `pnpm test`

This fixture contains `package.json`, `pnpm-lock.yaml`, a tiny JavaScript module, and a Node test script. PatchPilot should detect it through the Node/pnpm adapter before the broader npm adapter.
