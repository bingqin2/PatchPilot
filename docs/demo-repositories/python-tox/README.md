# Python Tox Demo Repository

Expected adapter: `python/tox`

Expected verification command:

```bash
tox
```

This fixture represents a Python repository that uses tox as the project-level test runner.
