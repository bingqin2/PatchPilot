# Node npm Demo

Adapter: Node/npm

Verification command: `npm test`

This fixture contains `package.json`, a tiny JavaScript module, and a Node test script. PatchPilot should detect it through the Node/npm adapter.
